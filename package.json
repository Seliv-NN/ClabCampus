{
  "name": "clubcampus",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "prestart": "mkdir -p /tmp/data && cp database.db /tmp/data/ 2>/dev/null || true",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "bcrypt": "^5.1.1",
    "express-session": "^1.17.3",
    "connect-sqlite3": "^0.9.13"
  },
  "engines": {
    "node": ">=18.x"
  }
}